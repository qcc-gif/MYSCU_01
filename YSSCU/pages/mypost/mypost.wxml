<!-- pages/mypost/mypost.wxml -->

        <scroll-view scroll-y="true" style="height: auto;" >
    <view wx:for="{{postList}}"wx:key="index">
    <adminstorPost postId=""
               profilePhoto="{{item.simgurl}}"
               name="{{item.name}}"
               position="{{item.position1}}"
               place="{{item.position2}}"
               studentNumber=""
               title="{{item.title}}"
               time="{{item.time}}"
               detail="{{item.detail}}"
               thumbnum="{{item.thumbnum}}"
               chatnum="{{item.chatnum}}"
               sharenum="{{item.sharenum}}"
               starnum="{{item.starnum}}">
    </adminstorPost>
</view>
</scroll-view>