<!--pages/fullText/fullText.wxml-->
<scroll-view scroll-y="true" style="height: 1430rpx;" >
<view wx:for="{{postList}}"wx:key="index">
    <fullText  label="{{item.position1}}"
               profilePhoto="{{item.simgurl}}"
               name="{{item.name}}"
               place="{{item.position2}}"
               title="{{item.ptitle}}"
               time="{{item.time}}"
               detail="{{item.detail}}"
               photo="{{item.profilePhoto}}"
               thumbnum="{{item.thumbnum}}"
               chatnum="{{item.chatnum}}"
               sharenum="{{item.sharenum}}"
               starnum="{{item.starnum}}">
    </fullText>
</view>
<view wx:for="{{commentList}}"wx:key="index">
    <comment   commentId="{{item.cid}}"
               profilePhoto="{{item.simgurl}}"
               name="{{item.name}}"
               time="{{item.time}}"
               detail="{{item.detail}}"
               photo="{{item.profilePhoto}}"
               thumbnum="{{item.thumbnum}}">
    </comment>
</view>
</scroll-view>


